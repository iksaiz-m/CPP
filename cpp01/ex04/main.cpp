#include "sed.hpp"


void sedforlosers(std::ofstream& file, std::string s1, std::string s2)
{
    file.
}

int main(std::string filename, std::string s1, std::string s2)
{
    std::ofstream file;
    // std::string amigo;
    // open(filename);
    file.open(filename);
    if (file.is_open())
        sedforlosers(file, s1, s2);
    // fopen(filename);
    // fread(filename)
    // .erase(s1)
    std::cin
     
}